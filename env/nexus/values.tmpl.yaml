{{- if  or (eq .Requirements.repository "nexus") (eq .Requirements.repository "") }}
enabled: true
persistence:
  enabled: true
  AccessMode: ReadWriteOnce
  Size: 30Gi
{{- else }}
enabled: false
{{- end }}
